FIND_PACKAGE(KDE4 REQUIRED)
INCLUDE(KDE4Defaults)
INCLUDE(MacroLibrary)
include_directories( ${KDE4_INCLUDES} )

add_subdirectory( libmediawiki )
add_subdirectory( plasmoid )
if( KIPI_PLUGIN )
add_subdirectory( kipi-plugin )
endif( KIPI_PLUGIN )
